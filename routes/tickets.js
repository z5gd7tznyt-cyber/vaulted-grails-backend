const express = require('express');
const router = express.Router();

// Placeholder for ticket purchase routes
router.get('/', (req, res) => {
  res.json({ message: 'Ticket purchase routes - Stripe integration coming soon' });
});

module.exports = router;
